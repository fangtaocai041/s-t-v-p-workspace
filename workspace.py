"""
⚠️ DEPRECATED — workspace.py 已被 workspace/ 目录遮蔽。

请使用 `from workspace import ...` (自动解析到 workspace/__init__.py)。
此文件保留仅作向后兼容，实际不会被 Python 导入。

新用法:
    from workspace import search_species, health_check
"""
