# Clash Verge 全局代理规则

**搜索前必须操作：Clash Verge → 代理模式 → 全局（Global）**

## 原因
学术搜索（PubMed / NCBI / Crossref / Google Scholar）在规则模式下会被阻断或超时。

## 操作流程
1. 搜索前：切为 **全局（Global）**
2. 搜索完成：切回 **规则（Rule）**

> 此规则同时适用于 fish-ecology-assistant/ 和 porpoise-agent/
